pipeline {
    agent any

    stages {
        stage() {
            steps {
                sh '''
                echo "===== JENKINS MASTER DETAILS ====="
                hostname
                hostname -I
                uname -a
                lscpu | head -10
                free -h
                df -h
                '''
            }
        }
    }
}
